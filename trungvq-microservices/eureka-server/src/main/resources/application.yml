server:
  port: 8761

spring:
  application:
    name: eureka-server
  zipkin:
    base-url: http://localhost:9411

eureka:
  client:
    fetch-registry: false
    # register it-self as a client
    register-with-eureka: false
    enable: false